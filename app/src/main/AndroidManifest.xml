<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rjtechltd.gengiskhan">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".BookingsActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".PendingActivity" />
        <activity android:name=".ExtraordinaryActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".RowLayoutPendingActivity" />
        <activity android:name=".RowLayoutAttendanceActivity" />
        <activity android:name=".RowLayoutFeedbackActivity"></activity>
    </application>

</manifest>